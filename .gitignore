.env
# Runtime-mutated live config — the dashboard writes secrets (e.g. anthropic_api_key)
# into it. Ship config/display.json.example as the template instead.
config/display.json
client-app
__pycache__/
*.py
*.md
*.claude
.codex
# Weekly report renderer (tools/report-pdf): deps and rendered output
tools/report-pdf/node_modules/
tools/report-pdf/out/
